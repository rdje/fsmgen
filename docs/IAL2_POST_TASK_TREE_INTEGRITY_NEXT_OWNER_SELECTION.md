# IAL2 Post-Task-Tree-Integrity Next-Owner Selection

## Selection

`IAL2-FEATURE-COMPLETENESS-FRONTIER.844` selects proposed no-product-behavior
`HIAL-VIAL-VERIFICATION-FIXTURE-ARCHITECTURE.1` as the next exact owner.

The selected leaf is a documentation/architecture audit. It must choose and
decompose the HIAL/VIAL topology, typed bridge, portable verification model,
typed native extensions, backend and simulator capability profiles, migration,
parity, traceability, scalability, validation, and rollback contracts before
any syntax, lowering, generated fixture, support-accounting, or runtime work.
This selector does not activate or execute that audit before its commit is
clean.

## Evidence Boundary

The authoritative IAL2 ledger is repaired and mechanically protected by clean
`.843` commit `c21765214`; `TASK-TREE-INTEGRITY` reports 844 numbered leaves,
844 unique root references, and one active tree / 845 total nodes. The private
SourceHIR boundary is complete under decision `0031`, and current public
support remains 332 protocol sources / 373 supported entries / 56 AHB sources,
split 28 generic `.ppif` / 28 `.ahb` aliases.

The remaining verification-generation gap is explicit and product-facing:
current IAL1 `(observe ...)` metadata emits inert UVM passive-monitor and VHDL
observation package skeletons, but no runnable stimulus, sampling, scenario,
expected-result, scoreboard, coverage, or fault-injection fixture. The parked
HIAL/VIAL requirement has a concrete handwritten AHB arbitration fixture as
architecture input, a director-established peer-intent requirement, no blocker,
and a first leaf that changes documentation/architecture only.

## Candidate Comparison

| Candidate | Result | Reason |
| --- | --- | --- |
| HIAL/VIAL architecture audit | **selected** | It is the strongest ungated product-architecture requirement after the private HIR and ledger prerequisites: a bounded no-behavior audit with a concrete verification gap, fixture, typed bridge, backend/profile, migration, parity, and scale questions already owned. |
| Public host-language builder | remains proposed | Decision `0031` requires a future explicit producer/public-projection choice, but private SourceHIR completion does not itself require publishing a builder. Language/API/package/versioning selection is an independent authoring product rather than a prerequisite for the HIAL/VIAL architecture audit. |
| End-to-end large-design scalability | remains proposed | This foundational requirement still needs workload profiles, correctness oracles, measurement schema, budgets, graceful failure, and gate split. HIAL/VIAL `.1` must define scale constraints for its own future artifacts; it does not replace later whole-product capacity qualification. |
| Beyond-read-only MCP | remains proposed | It crosses a distinct authorization/trust boundary and is not required to define verification intent or generated-fixture architecture. |
| Other protocols/backends and simulator profiles | folded only where selected | HIAL/VIAL `.1` owns the cross-backend and capability-profile architecture, not backend implementation. Existing protocol behavior and bounded VHDL support remain unchanged. |
| Transaction layering, lifecycle review, t1436, AXI-book, RAM guard, decision 0020 | ineligible | Each retains its explicit director/policy activation gate. Both frozen status files stay untouched. |

## Selected Audit Contract

After a separate clean activation, `HIAL-VIAL-VERIFICATION-FIXTURE-ARCHITECTURE.1`
must:

- determine whether VIAL needs VIAL0/VIAL1/VIAL2 or another minimum useful
  topology, without renaming current public IAL surfaces;
- freeze the typed language-neutral HIAL/VIAL bridge and portable semantics for
  stimulus, transactions, scenarios, concurrency, expected outcomes, temporal
  checks, reference models, scoreboards, coverage, and fault injection;
- define typed native extension points without cloning SystemVerilog/UVM or
  VHDL, and distinguish synthesizable HIAL output from verification-only VIAL;
- select capability-qualified portable SystemVerilog, full-language/UVM, VHDL,
  and mixed-language validation profiles without treating Verilator as full-LRM
  or UVM authority;
- map current checks/properties, `(observe ...)`, inert skeletons, and the AHB
  arbitration harness into migration/reuse and one worked architecture example;
- decompose source model, bridge, backend, tooling, validation, migration,
  documentation, parity, and scale proof into exact later leaves;
- change no parser, lowering, generated artifact, CLI/API/report/manifest,
  support accounting, HDL/UVM/VHDL, test runtime, or product behavior.

## Validation And Rollback

Validation covers the active-tree integrity gate, task/index and candidate
consistency scans, documentation audits, all mdBook chapters and HTML build,
Knowledge Map generation/check, memory, diff, exact repository-local cleanup,
and all registered doctrines.

Rollback removes this record/fact and the selection continuity, restores `.844`
to active, and removes only the newly selected HIAL/VIAL handoff. It changes no
product behavior in either direction.

Selector closeout passes active-tree integrity at one tree / 845 total nodes;
documentation audits at `Files=3, Tests=40`; all 36 mdBook chapters and the
72-file / 16,670,326-byte HTML build; Knowledge Map generation/check at 1,085
facts / 5,594 question keys; memory, diff, exact output cleanup, and all eight
doctrines.

Clean selector commit `031b21d4f` activates only selected architecture audit
`.1` through a separate continuity transition. The audit findings, topology,
bridge, verification semantics, profiles, migration, parity, scale contract,
implementation decomposition, and all product behavior remain unchanged until
that activation commits cleanly.
