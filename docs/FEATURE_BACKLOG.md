# Feature Backlog

The canonical list of user-visible features that are discussed as future work,
deferred, not fully shipped, or not yet fully frozen lives in the mdBook's
bounded backlog index and its directly linked topic pages:

- [docs/book/src/14-feature-backlog.md](book/src/14-feature-backlog.md)

Update the appropriate topic page whenever another book chapter or live
document adds a new "not fully shipped yet" caveat. This file exists as the
repository-level navigation pointer so the backlog is discoverable outside
the rendered book.
