# IAL2 Post-Import-Map Next-Owner Selection

## Selection

`IAL2-FEATURE-COMPLETENESS-FRONTIER.840` selects proposed no-behavior
`MDBOOK-AHB-BUSY-COUNT-RESIDUE-SYNC.1` as the next exact owner.

The selected leaf must synchronize one contradictory future/residue bullet in
Chapter 16c with the already shipped canonical decimal requester `busy-beats`
range `2..16`. This selector does not create or activate the child before its
own commit is clean.

## Reconciled Boundary

Clean identifier-era import-map completion commit `ae2f75648` aligns the
canonical entrypoint note/fact at `229` files / `228` packages / `19` IAL2
owners. Clean continuity commit `e129c7051` activates `.840` only.

AHB support accounting remains `332` protocol / `373` supported / `56` AHB
paths split `28` `.ppif` / `28` `.ahb`. Exact-one through exact-four public
requester fixtures remain the catalog surface; canonical literal counts
`5..16` deliberately reuse the same lowerer without one fixture per count.

## Contradiction Evidence

Chapter 16c currently states:

- at its mode table and requester sections, canonical literal counts `2..16`
  ship without a fixture per count and values above 16 remain deferred;
- in the completed generalized-count history, implementation `.3` ships
  `2..16` with focused 5/8/16 proof; but
- in a later future-direction bullet, counts beyond four remain outside the
  shipped surface.

The lowerer independently enforces canonical decimal `2..16` through
regex-equivalent `[2-9]|1[0-6]`, emits the same static-rule and residue wording,
and keeps absence as exact-one. The canonical shipped-behavior record agrees.
The later bullet is therefore stale documentation, not an ambiguous product
boundary.

## Candidate Comparison

| Candidate | Selection result | Evidence |
| --- | --- | --- |
| Chapter 16c busy-count residue sync | **selected** | One paragraph contradicts several current sections, the lowerer, and the shipped behavior record; the exact no-behavior repair is known. |
| HIAL/VIAL verification architecture | remains proposed | Foundational but much broader than one user-facing truth correction. |
| HIR / host-language builder | remains proposed | Product architecture work, not a prerequisite for the mdBook correction. |
| End-to-end scale and MCP-write horizon | remains proposed | Broader workload/API directions with independent owners. |
| Project-document lifecycle review | remains proposed | Decision `0025` keeps it and both frozen status files inactive. |
| Decision `0020` transaction architecture | ineligible | It remains a no-pivot North Star without director activation. |
| Other protocol/backend or simulator work | deferred | No evidence makes it a prerequisite for this documentation repair. |

## Selected Leaf Contract

After separate clean activation, the selected leaf must:

- change only Chapter 16c's stale future/residue bullet so it distinguishes
  exact-one-through-four public fixtures from shipped generic literal counts
  `5..16` without per-count fixtures;
- preserve literal values above 16, symbolic/policy/runtime/random selection,
  multiple insertion points, and all other listed residue;
- preserve runtime code, public sources, support accounting, generated
  artifacts, APIs, HDL/simulation/backend behavior, and broader roadmap owners;
- run focused chapter/book/status/path checks, full mdBook test/build, Knowledge
  Map, memory, diff, and doctrine gates using repository-local same-volume data.

## Validation And Rollback

Validation checks the corrected paragraph against the lowerer and canonical
behavior record, runs the focused documentation audits and full mdBook
test/build, and proves the diff changes no example or behavior-bearing file.

Selector closeout passes the feature-backlog status, live-book-path, and
relative-path audits with `Files=3, Tests=40`; the regenerated Knowledge Map
passes at `1074` facts / `5534` question keys, memory architecture passes with
`MEMORY.md` at `44` lines, and diff hygiene passes. Full mdBook validation is
reserved for the selected implementation leaf because this selector changes
no Chapter 16c content.

Rollback removes this selector record/fact and restores `.840` to active. The
stale Chapter 16c bullet and every product behavior remain unchanged until a
clean follow-up commit creates and activates the selected child.
