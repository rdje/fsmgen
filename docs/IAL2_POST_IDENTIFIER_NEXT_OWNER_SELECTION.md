# IAL2 Post-Identifier Next-Owner Selection

## Selection

`IAL2-FEATURE-COMPLETENESS-FRONTIER.839` selects proposed no-behavior
`BIN-FSMGEN-IMPORT-TREE-JUL30-IDENTIFIER-REFRESH.1` as the next exact owner.

The selected leaf must remeasure the live project-owned dependency closure
reachable from `bin/fsmgen`, synchronize the maintained architecture note and
its canonical fact after the portable identifier implementation added one
reachable Support owner, and change no runtime behavior. This selector does
not create or activate the selected child before its own commit is clean.

## Reconciled Boundary

Clean assertion-expectation completion commit `5fb1c0d47` closes the selected
identifier/precedence repair sequence without changing generated product
behavior in its final test-truth slice. Clean continuity commit `7fe11c74c`
activates `.839` only.

Public support accounting remains `332` protocol / `373` supported / `56` AHB
paths split `28` `.ppif` / `28` `.ahb`. HIAL/VIAL, HIR, host-language builder,
end-to-end scale, MCP-write horizon, protocols/backends, simulator profiles,
transaction layering, and every explicit director gate remain separate.

## Live Import Evidence

The exact activation-time `Module::ScanDeps` probe from repository root reports:

- `229` project-owned files total: `bin/fsmgen` plus `228` reachable
  `FSM::...` packages;
- `19` reachable IAL2 packages, unchanged from the canonical baseline;
- family count `Support 71`, with the remaining directory-family counts
  unchanged; and
- reachable `perl/FSM/Support/HDLInstanceIdentifierPolicy.pm`, which the
  maintained note does not yet link.

The canonical note and fact still record `228` total / `227` packages / `19`
IAL2 and `Support 70`. Git history traces the one-package movement exactly to
clean implementation commit `299db4cae`, which introduced the shared portable
identifier policy. This is explained post-baseline growth, not anomalous
dependency capture.

## Candidate Comparison

| Candidate | Selection result | Evidence |
| --- | --- | --- |
| Identifier-era `bin/fsmgen` import-tree refresh | **selected** | The canonical maintainer note/fact are exactly one reachable package stale; the introducing commit and bounded no-behavior correction are known. |
| Chapter 16c AHB residue truth repair | next independent candidate | The chapter currently ships and documents literal `2..16` in several current sections but later says counts beyond four remain outside the shipped surface. This user-facing contradiction is real, smaller, and separately ownerable; it must not be hidden or mixed into the import-map slice. |
| HIAL/VIAL verification architecture | remains proposed | Foundational but substantially broader than synchronizing one measured architecture baseline. |
| End-to-end large-design scalability | remains proposed | Required product direction, but broader than the exact no-behavior refresh. |
| Project-document lifecycle review | remains proposed | Decision `0025` keeps the scheduled review and both frozen status files inactive. |
| Decision `0020` transaction architecture | ineligible | It remains a no-pivot North Star without director activation. |
| Other protocol/backend or simulator work | deferred | No evidence makes it a prerequisite for the import-note truth repair. |

## Selected Leaf Contract

After separate clean activation, the selected leaf must:

- rerun `Module::ScanDeps` and record exact total, package, family, and IAL2
  closure counts;
- update `docs/BIN_FSMGEN_IMPORT_TREE.md` to `229` total / `228` packages,
  `Support 71`, and link the reachable portable identifier policy;
- update `docs/knowledge/bin-fsmgen-import-tree-current-baseline.md` and
  regenerate `KNOWLEDGE_MAP.md`;
- preserve runtime code, parser/generator behavior, public sources, support
  accounting, artifacts, APIs, HDL, simulation, backend behavior, and every
  broader roadmap owner; and
- run focused path/status, Knowledge Map, memory, diff, and doctrine gates with
  all project-owned data under repository-derived same-volume paths.

## Validation And Rollback

Validation reruns the exact closure/family/IAL2 probe, verifies the note and
fact against it, checks that every live package is represented honestly, and
runs focused documentation plus doctrine gates. No simulator or product
regression is required because the selected child changes documentation only.

Rollback removes this selector record/fact and restores `.839` to active. The
stale import note/fact and every product behavior remain unchanged until a
clean follow-up commit creates and activates the selected child.

## Closeout Evidence

- The exact live import probe reports `229` project files / `228` packages /
  `19` IAL2 owners; the added Support owner is traced to clean commit
  `299db4cae`.
- Feature-backlog status, live-book-path, and relative-path audits pass with
  `Files=3, Tests=40`.
- Knowledge Map generation/checking passes at `1,073` facts / `5,529` question
  keys; memory architecture passes with `MEMORY.md` at `48` lines, and diff
  hygiene passes.
