# ISF-TEMPORAL-CONTRACT-STORAGE-REPORTS: Temporal Contract Storage Report Roles

## Metadata

- Tree ID: `ISF-TEMPORAL-CONTRACT-STORAGE-REPORTS`
- Status: `done`
- Roadmap lane: `R14`
- Created: `2026-05-16`
- Last updated: `2026-05-16`
- Owner: repo-local workflow

## Goal

Expose temporal-contract monitor storage through bounded
`inferred_storage[].role` metadata so downstream reviewers can identify
contract pending, age, and sticky-fail storage without inspecting private
lowerer internals.

## Non-Goals

- Do not change temporal-contract runtime semantics, generated monitor DT
  behavior, reset behavior, or assertion projection.
- Do not freeze the whole schedule JSON schema.
- Do not add new temporal-contract source syntax.

## Acceptance Criteria

- Temporal-contract pending, age, and fail storage entries report a stable
  public role value.
- The public ISF contract and manifest metadata advertise the new role value.
- Focused coverage checks in-process and CLI schedule reports.
- ISF spec, downstream handoff, mdBook, task tree, roadmap/live docs, changes,
  and development notes are synchronized.
- The completed leaf is committed through `COMMIT.md`.

## Task Tree

- ID: `ISF-TEMPORAL-CONTRACT-STORAGE-REPORTS`
  Status: `done`
  Goal: `Expose temporal-contract monitor storage roles in schedule reports.`
  Children: `ISF-TEMPORAL-CONTRACT-STORAGE-REPORTS.1`

- ID: `ISF-TEMPORAL-CONTRACT-STORAGE-REPORTS.1`
  Status: `done`
  Goal: `Report temporal-contract monitor storage role metadata.`
  Acceptance: `Temporal-contract pending/fail registers and age counter carry an advertised role in schedule JSON, with public contract metadata and docs synchronized.`
  Verification: `perl syntax, focused public contract/report tests, ISF regression tier, mdBook build, and diff hygiene passed.`
  Commit: `pending`

## Current Frontier

| Order | Leaf | Status | Why next |
| --- | --- | --- | --- |
| 1 | `ISF-TEMPORAL-CONTRACT-STORAGE-REPORTS.1` | `done` | Completed; tree closed. |

## Decisions

- `2026-05-16`: Use one role value, `temporal_contract_monitor`, for the
  pending, age, and sticky-fail storage generated by a bounded eventual
  contract. The `temporal_contracts[]` summary already names each specific
  signal, so one storage-role family is enough for downstream filtering
  without over-promising subrole semantics.

## Open Questions

- None.

## Blockers

- None.

## Verification Log

| Date | Leaf | Checks | Result |
| --- | --- | --- | --- |
| `2026-05-16` | `ISF-TEMPORAL-CONTRACT-STORAGE-REPORTS.1` | `perl -Iperl -c perl/FSM/Scheduler/ISF/LoweringIR.pm`; `perl -Iperl -c perl/FSM/Support/ISFPublicInterfaceContract.pm`; `prove -Iperl t/1254-isf-temporal-contract-storage-report.t t/1148-isf-public-storage-metadata-audit.t t/1144-isf-public-tested-by-metadata-audit.t t/1225-isf-stage-contract-schedule-report.t`; `./bin/ci-regression isf`; `mdbook build docs/book`; `git diff --check` | `passed` |

## Commit Log

| Leaf | Commit subject or reference | Notes |
| --- | --- | --- |
| `ISF-TEMPORAL-CONTRACT-STORAGE-REPORTS.1` | `ISF-TEMPORAL-CONTRACT-STORAGE-REPORTS.1: report contract storage roles` | `pending commit` |

## Changelog

- `2026-05-16`: Created task tree and opened the first report-role leaf.
- `2026-05-16`: Completed the first leaf and closed the tree.
