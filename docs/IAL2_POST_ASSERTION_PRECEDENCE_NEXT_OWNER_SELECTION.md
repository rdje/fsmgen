# IAL2 Post Assertion-Precedence Next-Owner Selection

## Selection

`IAL2-FEATURE-COMPLETENESS-FRONTIER.833` selects proposed no-behavior truth-sync
leaf `MDBOOK-VHDL-INTRODUCTION-BOUNDARY-SYNC.1` as the next exact owner.

The selected leaf must align the mdBook introduction and backend-expectations
summary with the bounded direct/composition VHDL subset already documented in
Chapter 14. It must preserve the full-backend, external-compiler, GHDL, broad
composition, aggregate, package, and parity deferrals. This selector changes
no parser, lowering, generated artifact, CLI, report, support accounting, HDL,
test, or runtime behavior.

## Reconciled Shipped Boundary

Clean behavior commit `80aa203ab` completes the nested-bitwise concurrent-
assertion repair. Successful inline-intermediate substitution is explicitly
grouped, direct and temporal property leaves preserve their CoreAST semantics,
and the public AXI fixed-four admission set is unchanged. The behavior harness
proves exact `5/17/5/17/4` negative/positive execution and the separate legal-
only all-assertion harness proves exact `1/4/1/4/1` retirement.

The dated direct-VHDL facts establish that the CLI and pipeline already ship a
bounded direct single-FSM scaffold plus exact composition families. The direct
backend includes scalar/vector ports, resets, state/update structure, selected
arithmetic and aggregate forms, and explicit scalar/static-bit/vector unary-
reduction lowering. Exact C1/C2/APB-C4 composition subsets are also shipped.
Decision `0023` still forbids treating successful text generation as external
VHDL qualification: no authoritative VHDL compiler is installed, and full
backend/parity, broad composition, record/array/package emission, and GHDL
validation remain deferred.

AHB requester `busy-beats` remains canonical decimal `2..16`; public
accounting remains 332 protocol fixtures, 373 supported-smoke plus strict
fixtures, and 56 AHB paths split 28 `.ppif` / 28 `.ahb`. HIAL/VIAL, scale,
simulator profiles, startup maintenance beyond the selected book repair,
known defects, other protocols/backends, and decision `0020` remain
independently owned.

## Exact Public-Truth Drift

The mdBook currently contradicts itself:

- Chapter 00 says explicit VHDL support is intentionally not implemented.
- Chapter 10 says VHDL is recognized but not implemented as a full backend,
  then says composition VHDL is rejected before emission.
- Chapter 14 correctly says the VHDL backend is partially shipped and records
  the bounded direct and composition subsets plus their explicit deferrals.

The Chapter 10 phrase "not implemented as a full backend" is accurate only if
it is immediately paired with the shipped bounded subset. Its blanket
composition-rejection sentence is stale. Chapter 00's blanket non-
implementation claim is false. Because the mdBook is the user-facing source of
truth, this intra-book drift is the smallest current roadmap-integrity defect.

## Candidate Comparison

| Candidate | Selection result | Evidence |
| --- | --- | --- |
| mdBook VHDL introduction/backend boundary sync | **selected** | The live book makes mutually incompatible present-tense claims about an already-shipped backend subset. The exact two stale summaries and canonical Chapter 14 boundary are known; one no-behavior leaf owns the repair. |
| Frozen-legacy task-tree workflow sync | remains proposed | Decision `0007` and `COMMIT.md` are being followed, so no frozen blob is being modified. The stale workflow prose is real and should follow from another clean selector, but it does not outrank the book's current user-facing product falsehood. |
| `bin/fsmgen` import-tree refresh | remains proposed | The maintained architecture note's counts are stale, but this does not misstate supported product behavior at the book's front door. |
| HIAL/VIAL verification-fixture architecture | remains proposed | Director-endorsed and foundational, but its topology, typed bridge, portable/native semantics, two verification backends, simulator profiles, migration, parity, and scale decomposition are broader than the exact book truth repair. |
| End-to-end big/really-big scalability | remains proposed | Required product direction; measurable workload and capacity methodology remain broader than repairing current documentation drift. |
| Public-sync test drift | remains proposed | The three known sync failures remain independently owned. None is a prerequisite for the selected no-behavior book correction. |
| Remaining AHB/protocol/backend work | deferred | The shipped requester range and current accounting are stable; no adjacent behavior defect outranks the public book mismatch. |
| RAM-guard macOS metric refinement | not PNT-eligible | Correct capacity reporting is in use, but changing the safety guard still requires separate director authorization. |
| `IAL2-T1436-PREEXISTING-FAILURES` | not PNT-eligible | Its task explicitly requires director prioritization before activation. |
| Decision `0020` layered transactor roles | inactive by decision | The future North Star remains director-gated and is not PNT-eligible. |

## Selected Leaf Contract

`MDBOOK-VHDL-INTRODUCTION-BOUNDARY-SYNC.1` must:

- replace Chapter 00's blanket VHDL non-implementation statement with a concise
  bounded-subset/full-backend distinction and a Chapter 14 pointer;
- align Chapter 10's backend summary and composition wording with the shipped
  direct/C1/C2/APB-C4 subsets;
- preserve decision `0023`, the absent external compiler, GHDL deferral, full-
  backend/parity deferral, and unsupported-shape fail-closed boundaries;
- keep Chapter 14 canonical rather than duplicating its long feature matrix;
- run focused book truth/path/status checks, Knowledge Map, mdBook, diff, and
  doctrine gates; and
- make no product, test, source, generator, or runtime behavior change.

## Validation, Resources, And Rollback

Validation uses exact stale/correct wording scans, existing book status/path
audits, Knowledge Map generation/checking, a repository-local mdBook render,
diff hygiene, and all doctrine gates. No product regression is required for a
book-only correction whose shipped truth comes from dated fact cards and the
canonical Chapter 14 boundary.

Heavy commands use the authorized `--host-max-pct 100
--process-max-rss-mb 4096` profile with repository-derived same-volume
workspaces. Capacity truth uses the canonical Stats-compatible Mach-page
formula and reports kernel pressure separately; guard occupancy is not
capacity truth.

Rollback removes this selector record/fact and restores `.833` to active,
leaving the contradictory book wording and every shipped behavior unchanged.
A clean selector commit may activate only the selected book-sync leaf through
a separate continuity commit.

Clean selector commit `191a65151` activates only
`MDBOOK-VHDL-INTRODUCTION-BOUNDARY-SYNC.1` through continuity changes. The
contradictory summaries and every shipped product behavior remain unchanged
during activation.

## Closeout Evidence

- Book/status/path truth passes 5 files/329 tests. Knowledge Map generation and
  checking passes at 1,062 facts/5,466 question keys.
- The mdBook renders exactly 72 files/16,526,790 bytes; its exact repository-
  local output is removed, and `.artifacts/tmp/tests` is empty.
- `MEMORY.md` is 49 lines, `README.md` is 2,349 lines, diff hygiene passes, and
  all six doctrine gates pass, including project-data locality.
- Final canonical Stats-compatible capacity is
  18,141,708,288/25,769,803,776 bytes = 16.896/24.000 GiB = 70.40%, with
  separate macOS kernel pressure level 1 and `memory_pressure` 75% free. Guard
  occupancy is excluded from capacity truth. No background job remains.
