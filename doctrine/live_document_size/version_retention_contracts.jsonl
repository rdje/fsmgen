{"record_type":"registry","schema_version":1,"max_records":16,"max_bytes":8192}
{"record_type":"contract","schema_version":1,"contract_id":"fsmgen_required_history","owner":"repository-maintainers","guarantee":"Every referenced revision and path remains reachable from the authoritative repository or an owner-controlled full-history backup; rewrites must preserve or materialize required objects before publication.","recovery":"Fetch complete history from the authoritative remote; if a rewrite removed the object, restore it from the owner-controlled full-history backup or replace the descriptor with a content-addressed repository file, then rerun the gate."}
